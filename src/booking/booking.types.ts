export enum BookingTypeEnum {
  PENDING = 'pending',
  ACCEPTED_BY_STAR = 'accepted_by_star',
  REJECTED_BY_STAR = 'rejected_by_star',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  CANCELLED = 'cancelled',
}

export enum DateRangeType {
  WEEKLY = 'week',
  MONTHLY = 'month',
}
